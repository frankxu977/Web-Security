# Network Foundations - Network Concepts

## 1. Overview

这一节最重要的内容有四块：

```text
OSI Model
TCP/IP Model
Protocols
Transmission
```

核心目标：

> 理解数据从一个设备发送到另一个设备时，网络中的不同层分别负责什么。

---

# 2. OSI Model

OSI 全称：

**Open Systems Interconnection Model**

它是一个用来理解网络通信的 **7 层理论模型**。

```text
7. Application
6. Presentation
5. Session
4. Transport
3. Network
2. Data Link
1. Physical
```

可以把它理解为：

```text
用户程序
   ↓
数据处理
   ↓
建立通信
   ↓
可靠传输
   ↓
寻找路线
   ↓
局域网传输
   ↓
真实电信号 / 光信号
```

---

# 3. OSI Layer 1 - Physical Layer

## Physical Layer
**物理层**

负责：

> 真正把 0 和 1 通过物理介质发送出去。

常见设备 / 技术：

- Ethernet Cable
- Fiber Optic
- Hub
- Repeater
- Radio Signal

数据形式：

```text
Bits
0 1 0 1 1 0 1
```

Example:

```text
Computer
   │
Ethernet Cable
   │
Switch
```

这里真正通过网线传输电信号的部分属于：

**Layer 1**

---

# 4. OSI Layer 2 - Data Link Layer

## Data Link Layer
**数据链路层**

负责：

> 两个直接连接的网络节点之间的数据传输。

重要概念：

```text
MAC Address
Frame
Switch
```

### MAC Address

MAC Address 用于识别同一局域网中的网络设备。

Example:

```text
00:1A:2B:3C:4D:5E
```

### Switch

Switch 主要工作在 Layer 2。

```text
PC A
 │
 │
Switch
 │
 │
PC B
```

Switch 根据：

```text
MAC Address
```

决定数据应该发送给哪个设备。

---

# 5. OSI Layer 3 - Network Layer

## Network Layer
**网络层**

负责：

> 数据跨不同网络传输，以及决定数据应该走哪条路径。

重要概念：

```text
IP Address
Packet
Routing
Router
```

### IP Address

用于逻辑寻址。

Example:

```text
192.168.1.10
10.10.14.5
```

### Router

Router 主要工作在：

```text
Layer 3
```

Router 根据 IP Address 决定数据的路径。

Example:

```text
Your PC
   ↓
Router
   ↓
Internet
   ↓
Router
   ↓
Web Server
```

---

# 6. Layer 2 vs Layer 3

这个非常重要。

```text
Layer 2
→ MAC Address
→ Switch
→ Local Network

Layer 3
→ IP Address
→ Router
→ Between Networks
```

简单记：

```text
MAC
→ 找局域网里的设备

IP
→ 找不同网络里的设备
```

---

# 7. OSI Layer 4 - Transport Layer

## Transport Layer
**传输层**

负责：

> 应用程序之间的端到端数据传输。

核心协议：

```text
TCP
UDP
```

同时负责：

- Segmentation
- Reassembly
- Flow Control
- Error Checking

---

## TCP

**Transmission Control Protocol**

特点：

```text
Reliable
Connection-oriented
Ordered
Error Recovery
```

也就是：

> TCP 更在意数据有没有正确送到。

例如：

```text
Web Pages
File Transfer
Login
```

很多情况下需要：

```text
数据完整
+
数据顺序正确
```

因此适合 TCP。

---

## UDP

**User Datagram Protocol**

特点：

```text
Fast
Connectionless
No delivery guarantee
```

也就是：

> UDP 更在意速度，不保证每一个数据包都送到。

常见用途：

```text
Streaming
Gaming
VoIP
DNS
```

---

# 8. TCP vs UDP

```text
TCP
→ Reliable
→ Connection-oriented
→ Slower
→ 保证顺序
→ 错误恢复

UDP
→ Faster
→ Connectionless
→ No guarantee
→ 不保证顺序
→ 不负责错误恢复
```

简单理解：

```text
TCP = 快递签收
UDP = 扔过去就走
```

---

# 9. OSI Layer 5 - Session Layer

## Session Layer
**会话层**

负责：

```text
建立 Session
维护 Session
终止 Session
```

Session 就是两个程序之间持续的一段通信。

例如：

```text
Client
   ↓
Session Established
   ↓
Server

持续交换数据

   ↓
Session Closed
```

---

# 10. OSI Layer 6 - Presentation Layer

## Presentation Layer
**表示层**

负责：

> 将数据转换成双方应用程序都可以理解的格式。

主要功能：

```text
Encryption
Decryption
Compression
Data Format Conversion
```

例如：

```text
Original Data
↓
Encrypt
↓
Encrypted Data
↓
Network
↓
Decrypt
↓
Original Data
```

---

# 11. OSI Layer 7 - Application Layer

## Application Layer
**应用层**

最接近用户。

为应用程序提供网络服务。

常见协议：

```text
HTTP
FTP
SMTP
DNS
```

例如：

```text
Browser
↓
HTTP
↓
Web Server
```

---

# 12. OSI Model Summary

```text
Layer 7 - Application
HTTP / FTP / SMTP / DNS
应用程序网络服务

Layer 6 - Presentation
Encryption / Compression / Formatting
数据格式转换

Layer 5 - Session
Create / Maintain / Close Sessions
管理会话

Layer 4 - Transport
TCP / UDP
端到端传输

Layer 3 - Network
IP / Router
路由和逻辑地址

Layer 2 - Data Link
MAC / Switch / Frame
节点之间传输

Layer 1 - Physical
Cable / Fiber / Signal
真实比特传输
```

---

# 13. Important OSI Associations

这几个建议直接记住：

```text
Layer 1
→ Bits

Layer 2
→ Frame
→ MAC Address
→ Switch

Layer 3
→ Packet
→ IP Address
→ Router

Layer 4
→ Segment
→ TCP / UDP
```

---

# 14. Sending a File Through OSI

假设你发送一个文件：

```text
Application Layer
↓
应用程序发起文件传输

Presentation Layer
↓
加密 / 压缩 / 格式转换

Session Layer
↓
建立通信 Session

Transport Layer
↓
将数据分成 Segments
TCP / UDP

Network Layer
↓
加入 IP 信息
确定 Route

Data Link Layer
↓
形成 Frames
加入 MAC 地址

Physical Layer
↓
变成 Bits
通过电缆 / Wi-Fi 发送
```

接收端再：

```text
Layer 1
↑
Layer 2
↑
Layer 3
↑
Layer 4
↑
Layer 5
↑
Layer 6
↑
Layer 7
```

把数据重新还原。

这个过程叫：

```text
Encapsulation
封装
```

接收端反过来叫：

```text
Decapsulation
解封装
```

---

# 15. TCP/IP Model

TCP/IP 是现实互联网真正广泛使用的模型。

它只有：

```text
4 Layers
```

分别是：

```text
Application
Transport
Internet
Link
```

---

# 16. TCP/IP Link Layer

## Link Layer

对应 OSI：

```text
OSI Layer 1
+
OSI Layer 2
```

负责：

- Physical Connection
- Ethernet
- Wi-Fi
- MAC Address
- Frames

```text
Physical + Data Link
↓
TCP/IP Link Layer
```

---

# 17. TCP/IP Internet Layer

## Internet Layer

对应：

```text
OSI Layer 3
```

核心协议：

```text
IP
ICMP
```

负责：

```text
IP Addressing
Routing
Packet Delivery
```

---

# 18. TCP/IP Transport Layer

对应：

```text
OSI Layer 4
```

核心协议：

```text
TCP
UDP
```

负责：

```text
End-to-End Communication
```

---

# 19. TCP/IP Application Layer

对应 OSI：

```text
Layer 5
Layer 6
Layer 7
```

合并成：

```text
Application Layer
```

常见协议：

- HTTP
- FTP
- SMTP
- DNS

---

# 20. OSI vs TCP/IP

对应关系：

```text
OSI                     TCP/IP

Application      ┐
Presentation     ├──→   Application
Session          ┘

Transport        ───→   Transport

Network          ───→   Internet

Data Link        ┐
Physical         ┘──→   Link
```

---

# 21. OSI vs TCP/IP 的作用

### OSI

更像：

```text
理论模型
```

主要帮助：

- 学习网络
- 理解网络结构
- Troubleshooting
- 分析网络问题

---

### TCP/IP

更像：

```text
现实世界真正使用的网络模型
```

Internet 实际上主要基于：

```text
TCP/IP
```

所以：

```text
OSI
→ 理解网络

TCP/IP
→ 实际运行网络
```

---

# 22. Visiting a Website Using TCP/IP

当你打开一个网站：

```text
Browser
↓
HTTP Request
```

### Application Layer

```text
HTTP
```

浏览器生成 HTTP Request。

↓

### Transport Layer

```text
TCP
```

TCP 保证可靠传输。

↓

### Internet Layer

```text
IP
```

IP 决定数据应该发送到哪个服务器。

↓

### Link Layer

```text
Ethernet / Wi-Fi
```

数据通过真实网络发送。

最终：

```text
Browser
↓
HTTP
↓
TCP
↓
IP
↓
Wi-Fi / Ethernet
↓
Internet
↓
Web Server
```

---

# 23. Protocol

## Protocol
**网络协议**

Protocol 是：

> 网络设备之间通信时必须遵守的一套规则。

就像人类交流需要使用共同语言。

例如：

```text
Browser
↓
HTTP
↓
Web Server
```

双方都理解 HTTP，所以才能交流。

---

# 24. Common Protocols

## HTTP

**Hypertext Transfer Protocol**

用途：

```text
Web Browsing
```

工作层：

```text
Application Layer
```

Example:

```http
GET /index.html HTTP/1.1
Host: example.com
```

---

## FTP

**File Transfer Protocol**

用途：

```text
File Transfer
```

例如：

```text
Upload File
Download File
```

工作在：

```text
Application Layer
```

---

## SMTP

**Simple Mail Transfer Protocol**

用途：

```text
Email Sending
```

例如：

```text
Mail Server
↓
SMTP
↓
Mail Server
```

---

## TCP

用途：

```text
Reliable Transport
```

工作在：

```text
Transport Layer
```

---

## UDP

用途：

```text
Fast Connectionless Transport
```

工作在：

```text
Transport Layer
```

---

## IP

**Internet Protocol**

用途：

```text
Addressing
Routing
```

负责：

> 把 Packet 从源网络送到目标网络。

---

# 25. Protocol Summary

```text
HTTP
→ Web

FTP
→ File Transfer

SMTP
→ Email

TCP
→ Reliable Transport

UDP
→ Fast Transport

IP
→ Addressing + Routing

DNS
→ Domain Name → IP Address

ICMP
→ Network Control / Diagnostics
```

---

# 26. Transmission

Transmission 指：

> 数据从一个设备通过某种介质发送到另一个设备的过程。

主要需要知道：

```text
Transmission Type
Transmission Mode
Transmission Medium
```

---

# 27. Transmission Types

两类：

```text
Analog
Digital
```

---

## Analog Transmission

使用：

```text
Continuous Signal
```

Example:

```text
Traditional Radio
```

---

## Digital Transmission

使用：

```text
0 and 1
Discrete Signals
```

现代计算机网络主要使用：

```text
Digital Transmission
```

---

# 28. Transmission Modes

三种：

```text
Simplex
Half-Duplex
Full-Duplex
```

---

## Simplex

只能：

```text
A → B
```

不能反过来。

Example:

```text
Keyboard
   ↓
Computer
```

---

## Half-Duplex

可以双向：

```text
A ↔ B
```

但是：

> 不能同时发送。

Example:

```text
Walkie-Talkie
```

你说话的时候对方需要等。

---

## Full-Duplex

可以：

```text
A ⇄ B
```

双方同时发送和接收。

Example:

```text
Phone Call
```

---

# 29. Transmission Mode Summary

```text
Simplex
A → B

Half-Duplex
A → B
然后
A ← B

Full-Duplex
A ⇄ B
```

---

# 30. Transmission Media

## Wired

有线传输：

```text
Twisted Pair
Coaxial Cable
Fiber Optic
```

---

### Twisted Pair

常用于：

```text
Ethernet
LAN
```

比如普通网线。

---

### Coaxial Cable

常见于：

```text
Cable TV
Older Ethernet
```

---

### Fiber Optic

通过：

```text
Light
```

传输数据。

特点：

```text
High Speed
Long Distance
High Bandwidth
```

常用于互联网 Backbone。

---

# 31. Wireless Media

无线传输包括：

```text
Radio Waves
Microwaves
Infrared
```

### Radio Waves

Examples:

```text
Wi-Fi
Cellular Network
```

### Microwaves

Example:

```text
Satellite Communication
```

### Infrared

Example:

```text
Remote Control
```

---

# 32. Cybersecurity Relevance

网络安全中理解这些内容非常重要。

例如扫描目标时：

```text
Nmap
↓
Find IP
↓
Find Ports
↓
Find Services
```

涉及：

```text
Layer 3
→ IP

Layer 4
→ TCP / UDP / Ports

Layer 7
→ HTTP / FTP / SSH 等服务
```

---

## Example

如果 Nmap 发现：

```text
10.10.10.5

Port 80/TCP Open
HTTP
```

可以理解为：

```text
IP Address
→ Layer 3

TCP
→ Layer 4

Port 80 / HTTP
→ Application Service
```

这就是为什么 Pentester 必须理解 OSI / TCP-IP。

---

# Quick Summary

```text
OSI Model
→ 7 Layers
→ 理论模型

TCP/IP
→ 4 Layers
→ 现实互联网使用
```

OSI：

```text
7 Application
6 Presentation
5 Session
4 Transport
3 Network
2 Data Link
1 Physical
```

关键对应：

```text
L2
→ MAC
→ Switch
→ Frame

L3
→ IP
→ Router
→ Packet

L4
→ TCP / UDP
→ Segment

L7
→ HTTP / FTP / SMTP / DNS
```

TCP/IP：

```text
Application
Transport
Internet
Link
```

Protocols：

```text
HTTP → Web
FTP → Files
SMTP → Email
TCP → Reliable
UDP → Fast
IP → Routing
DNS → Domain → IP
```

Transmission：

```text
Simplex
→ One Way

Half-Duplex
→ Both Ways, Not Simultaneously

Full-Duplex
→ Both Ways Simultaneously
```

Media：

```text
Wired
→ Twisted Pair
→ Coaxial
→ Fiber

Wireless
→ Radio
→ Microwave
→ Infrared
```

---

# Key Points to Remember

```text
OSI = 7 Layers

TCP/IP = 4 Layers

Layer 2 = MAC + Switch

Layer 3 = IP + Router

Layer 4 = TCP / UDP

Layer 7 = HTTP / FTP / SMTP / DNS

TCP = Reliable

UDP = Fast

Simplex = One Way

Half-Duplex = Two Way, One at a Time

Full-Duplex = Two Way, Same Time
```
