# Network Foundations - Network Communication

这一节最核心的三个概念：

```text
MAC Address
IP Address
Port
```

可以先这样理解：

```text
MAC Address
→ 找到“本地网络里的哪台设备”

IP Address
→ 找到“网络里的哪台主机”

Port
→ 找到“这台主机上的哪个服务/程序”
```

---

# 1. MAC Address

## What is a MAC Address?

MAC 全称：

```text
Media Access Control Address
```

MAC 地址是：

> 分配给网络接口卡 NIC 的标识，用于本地网络中的设备识别和通信。

工作在：

```text
OSI Layer 2
Data Link Layer
```

---

## MAC Address Format

MAC 地址长度：

```text
48 bits
```

通常写成 6 组十六进制：

```text
00:1A:2B:3C:4D:5E
```

每组：

```text
8 bits
```

所以：

```text
6 × 8 = 48 bits
```

---

## MAC Address Structure

一个 MAC 地址可以粗略分成：

```text
First 24 bits
→ Manufacturer / OUI

Last 24 bits
→ Specific Device
```

OUI：

```text
Organizationally Unique Identifier
```

用于标识厂商。

---

# 2. NIC and MAC

MAC 地址通常与：

```text
NIC
Network Interface Card
```

相关。

例如：

```text
Laptop
↓
Wi-Fi NIC
↓
MAC Address
```

或者：

```text
Desktop
↓
Ethernet NIC
↓
MAC Address
```

Windows 中可以使用：

```cmd
getmac
```

查看网卡的 MAC 地址。

---

# 3. How MAC Address is Used

MAC 地址主要用于：

> 同一个 LAN 内，把 Frame 送到正确的物理设备。

例如：

```text
Computer A
IP: 192.168.1.2
MAC: AA:AA:AA:AA:AA:AA

Computer B
IP: 192.168.1.5
MAC: BB:BB:BB:BB:BB:BB
```

如果 A 要给 B 发数据：

```text
A knows B's IP
↓
Find B's MAC
↓
Build Frame
↓
Destination MAC = B's MAC
↓
Switch forwards Frame
↓
B receives data
```

---

# 4. ARP

## ARP - Address Resolution Protocol

ARP 的作用：

```text
IP Address
↓
Find corresponding MAC Address
```

也就是：

> 已经知道对方 IP，但是不知道对方 MAC 时，使用 ARP。

例如：

```text
192.168.1.5
↓ ARP
BB:BB:BB:BB:BB:BB
```

所以 ARP 是连接：

```text
Layer 3 IP
和
Layer 2 MAC
```

之间的重要协议。

---

# 5. IP Address

## What is an IP Address?

IP 全称：

```text
Internet Protocol Address
```

IP 地址是：

> 分配给网络设备的逻辑地址，用于跨网络定位和通信。

工作在：

```text
OSI Layer 3
Network Layer
```

---

# 6. IPv4

IPv4 长度：

```text
32 bits
```

Example:

```text
192.168.1.1
```

格式：

```text
4 个十进制数字
用 . 分隔
```

---

# 7. IPv6

IPv6 长度：

```text
128 bits
```

Example:

```text
2001:0db8:85a3:0000:0000:8a2e:0370:7334
```

IPv6 出现的重要原因：

```text
IPv4 Address Exhaustion
```

也就是 IPv4 地址数量不够用了。

---

# 8. MAC vs IP

这个非常重要。

```text
MAC Address
→ Layer 2
→ Local Network
→ Physical / Interface Identification

IP Address
→ Layer 3
→ Across Networks
→ Logical Addressing
```

简单理解：

```text
MAC
→ 本地找到具体设备

IP
→ 跨网络找到目标主机
```

---

# 9. Router and IP

Router 使用：

```text
IP Address
```

来决定 Packet 应该往哪里发送。

例如：

```text
Your PC
↓
Router
↓
Internet
↓
Router
↓
Web Server
```

每个 Router 都根据：

```text
Destination IP
```

决定下一跳。

---

# 10. Port

## What is a Port?

Port 是一个数字，用于：

> 区分同一台计算机上的不同网络服务或应用程序。

工作在：

```text
OSI Layer 4
Transport Layer
```

和：

```text
TCP
UDP
```

一起使用。

---

# 11. Why Do We Need Ports?

假设一台服务器同时运行：

```text
Website
SSH
Email
Database
```

它们都可以使用：

```text
同一个 IP Address
```

那怎么区分？

靠：

```text
Port
```

例如：

```text
IP: 10.10.10.5

Port 22
→ SSH

Port 80
→ HTTP

Port 443
→ HTTPS
```

所以：

```text
IP
→ 哪台机器

Port
→ 哪个服务
```

---

# 12. Common Ports

```text
20 / 21
→ FTP

22
→ SSH

25
→ SMTP

53
→ DNS

80
→ HTTP

443
→ HTTPS

1433
→ Microsoft SQL Server
```

---

# 13. Port Range

Port 范围：

```text
0 - 65535
```

分成三个主要类别。

---

# 14. Well-Known Ports

范围：

```text
0 - 1023
```

用于常见标准服务。

Examples:

```text
21
→ FTP

22
→ SSH

80
→ HTTP

443
→ HTTPS
```

这些主要由：

```text
IANA
Internet Assigned Numbers Authority
```

管理。

---

# 15. Registered Ports

范围：

```text
1024 - 49151
```

通常由：

```text
特定软件
特定服务
应用程序
```

使用。

例如：

```text
1433
→ Microsoft SQL Server
```

---

# 16. Dynamic / Private / Ephemeral Ports

范围：

```text
49152 - 65535
```

也叫：

```text
Ephemeral Ports
Temporary Ports
Dynamic Ports
```

通常由客户端操作系统临时分配。

例如：

```text
Your Browser

Source Port:
55000

Destination Port:
443
```

你访问 HTTPS 网站时：

```text
Client
55000
    ↓
Server
443
```

服务器响应时：

```text
Server
443
    ↓
Client
55000
```

---

# 17. Client Port vs Server Port

通常：

```text
Server
→ Fixed Port

Client
→ Temporary / Ephemeral Port
```

例如访问网站：

```text
Client IP: 192.168.1.10
Client Port: 53000

↓

Server IP: 93.184.216.34
Server Port: 443
```

完整连接可以表示成：

```text
192.168.1.10:53000
→
93.184.216.34:443
```

---

# 18. IP + Port

非常重要：

```text
IP Address
+
Port
```

可以帮助唯一确定：

> 某台主机上的某个网络服务。

Example:

```text
10.10.10.5:22
```

意思：

```text
10.10.10.5
→ Host

22
→ SSH Service
```

---

# 19. Browsing a Website - Full Process

这是这一节最重要的整体流程。

假设你访问：

```text
example.com
```

---

## Step 1 - DNS Lookup

浏览器首先需要知道：

```text
example.com
```

对应哪个：

```text
IP Address
```

例如：

```text
example.com
↓ DNS
93.184.216.34
```

---

# 20. Step 2 - Browser Creates Request

浏览器生成：

```text
HTTP Request
```

例如：

```http
GET / HTTP/1.1
Host: example.com
```

如果是 HTTPS：

```text
Destination Port = 443
```

HTTP：

```text
Destination Port = 80
```

---

# 21. Step 3 - TCP

HTTP 数据交给 TCP。

TCP 添加：

```text
Source Port
Destination Port
```

例如：

```text
Source Port:
53000

Destination Port:
443
```

---

# 22. Step 4 - IP

然后加入：

```text
Source IP
Destination IP
```

Example:

```text
Source:
192.168.1.10

Destination:
93.184.216.34
```

---

# 23. Step 5 - ARP

你的电脑发现：

```text
Destination IP
不在自己的 Local Network
```

所以要把数据先交给：

```text
Default Gateway
Router
```

但发送 Frame 需要：

```text
Router MAC Address
```

因此：

```text
Router IP
↓ ARP
Router MAC
```

---

# 24. Step 6 - Frame

电脑构建 Layer 2 Frame：

```text
Destination MAC
→ Router MAC

Source MAC
→ Your NIC MAC
```

然后发送：

```text
Frame
↓
Router
```

---

# 25. Step 7 - Routing

Router 查看：

```text
Destination IP
```

然后：

```text
Routing Table
↓
Select Route
↓
Forward Packet
```

中间可能经过很多 Router：

```text
Your Router
↓
ISP Router
↓
Internet Router
↓
Another Router
↓
Server Network
```

---

# 26. Step 8 - Server Receives Packet

Server 收到 Packet：

```text
Destination IP
→ This Server
```

然后查看：

```text
Destination Port
```

例如：

```text
443
```

于是将数据交给：

```text
HTTPS / Web Server Application
```

---

# 27. Step 9 - Server Responds

服务器处理：

```text
HTTP Request
```

然后生成：

```text
HTTP Response
```

响应发送回：

```text
Client IP
+
Client Ephemeral Port
```

例如：

```text
192.168.1.10:53000
```

---

# 28. Full Web Request Flow

整个过程：

```text
example.com
↓
DNS Lookup
↓
93.184.216.34
↓
HTTP Request
↓
TCP
Port 443
↓
IP
93.184.216.34
↓
ARP
Find Router MAC
↓
Ethernet Frame
↓
Router
↓
Internet
↓
Web Server
↓
Port 443
↓
HTTP Server
↓
Response
↓
Back to Client
```

---

# 29. Three Addresses You Need to Understand

网络通信可以看成三层定位：

```text
MAC Address
→ Which local device?

IP Address
→ Which host/network?

Port
→ Which application/service?
```

例如：

```text
MAC
→ 找到 Router

IP
→ 找到 Web Server

Port 443
→ 找到 HTTPS Service
```

---

# 30. Cybersecurity Relevance

以后做 Pentesting，这三个概念会一直出现。

例如：

```text
Target:
10.10.10.5
```

先确定：

```text
IP Address
```

然后 Nmap：

```bash
nmap 10.10.10.5
```

发现：

```text
22/tcp open
80/tcp open
443/tcp open
```

意味着：

```text
22
→ SSH

80
→ HTTP

443
→ HTTPS
```

然后进一步分析这些服务有没有漏洞。

所以 Pentesting 经常是：

```text
Find IP
↓
Scan Ports
↓
Identify Services
↓
Enumerate
↓
Find Vulnerabilities
```

---

# Quick Summary

```text
MAC Address
→ Layer 2
→ Local Network
→ NIC
→ Switch
→ Frame

IP Address
→ Layer 3
→ Across Networks
→ Router
→ Packet

Port
→ Layer 4
→ Application / Service
→ TCP / UDP
```

---

# Important Numbers

```text
MAC
→ 48 bits

IPv4
→ 32 bits

IPv6
→ 128 bits

Ports
→ 0 - 65535
```

---

# Port Ranges

```text
0 - 1023
→ Well-Known Ports

1024 - 49151
→ Registered Ports

49152 - 65535
→ Dynamic / Private / Ephemeral Ports
```

---

# Common Ports

```text
20 / 21
→ FTP

22
→ SSH

25
→ SMTP

53
→ DNS

80
→ HTTP

443
→ HTTPS

1433
→ MSSQL
```

---

# Key Points to Remember

```text
MAC
→ Local device

IP
→ Network host

Port
→ Service / Application

ARP
→ IP to MAC

Switch
→ MAC

Router
→ IP

TCP / UDP
→ Ports

DNS
→ Domain to IP
```

---

# Most Important Mental Model

```text
I want to access:

https://example.com
```

Network thinks:

```text
DNS:
example.com
→ What IP?

IP:
Which computer?

Port:
Which service?

MAC:
Which local device gets the next frame?
```

最终：

```text
Domain
↓
IP
↓
Port
↓
MAC
↓
Physical Transmission
```
