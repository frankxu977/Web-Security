# Network Foundations - Introduction

## 1. What is a Network?

A **network（网络）** is a group of interconnected devices that can communicate with each other, send/receive data, and share resources.

常见设备包括：

- Computers
- Smartphones
- Printers
- Servers
- Routers
- Switches

这些连接到网络中的单个设备通常称为：

**Nodes（节点）**

```text
PC
 │
 ├── Phone
 │
 ├── Printer
 │
 └── Server
```

只要这些设备能够彼此通信，就可以形成一个 Network。

---

# 2. Why Networks Matter

网络的核心作用：

```text
Communication
+
Data Sharing
+
Resource Sharing
```

例如：

```text
Laptop
   ↓
Network
   ↓
Server
```

你的电脑可以通过网络访问服务器提供的：

- Websites
- Files
- Applications
- Databases
- Cloud Services

现代计算机系统几乎都依赖网络，因此理解 **Networking** 是学习 Cybersecurity 的基础。

---

# 3. Key Network Concepts

## Nodes

**Node = 连接到网络中的单个设备**

Examples:

```text
Computer
Phone
Server
Printer
Router
```

例如：

```text
Laptop = Node
Server = Node
Printer = Node
```

---

## Links

**Link = 连接节点之间的通信路径**

Link 可以是：

- Wired（有线）
- Wireless（无线）

例如：

```text
Laptop
   │
 Wi-Fi
   │
Router
```

这里的 Wi-Fi connection 就是一个 **Link**。

再比如：

```text
PC
 │
Ethernet Cable
 │
Switch
```

Ethernet Cable 也是一个 Link。

---

## Data Sharing

网络最主要的目的之一就是：

**在设备之间交换数据。**

例如：

```text
Client
   │
   │ Request
   ↓
Server
   │
   │ Response
   ↓
Client
```

你访问网站时，本质上就是：

```text
你的电脑
↓
发送 Request
↓
Web Server
↓
返回 Response
↓
你的电脑
```

---

# 4. LAN and WAN

这一模块主要会学习两种重要网络：

```text
LAN
Local Area Network

WAN
Wide Area Network
```

---

## LAN - Local Area Network

**局域网**

覆盖范围比较小。

Examples:

- Home Network
- School Network
- Office Network

例如：

```text
        Home LAN

Laptop ──┐
Phone ───┤
TV ──────┤── Router
Printer ─┘
```

这些设备通常位于：

```text
同一个家庭
同一个办公室
同一栋建筑
```

---

## WAN - Wide Area Network

**广域网**

连接距离较远的多个网络。

例如：

```text
LAN
 │
 │
WAN
 │
 │
LAN
```

最典型的例子：

**The Internet**

可以简单理解为：

```text
Internet
=
大量网络连接在一起
```

也就是：

```text
Network of Networks
```

---

# 5. Basic Network Structure

最基本的网络结构：

```text
Client
   ↓
Local Network
   ↓
Router
   ↓
Internet
   ↓
Server
```

例如访问 Hack The Box：

```text
你的电脑
   ↓
Wi-Fi / Ethernet
   ↓
Router
   ↓
Internet
   ↓
HTB Server
```

---

# 6. Networking and Cybersecurity

学习网络安全之前，必须先理解网络。

因为很多攻击和防御都发生在：

```text
Device
↓
Network
↓
Server
```

Pentester 经常需要理解：

- Which devices exist?
- Which systems are reachable?
- Which ports are open?
- Which services are running?
- How do devices communicate?
- How is the network divided?

例如：

```text
Target Server
↓
Port 22 Open
↓
SSH Service
```

或者：

```text
Target Server
↓
Port 80 Open
↓
HTTP Web Server
```

所以 Networking 是以后学习以下内容的基础：

```text
Nmap
Wireshark
Enumeration
Pivoting
Active Directory
Network Pentesting
```

---

# 7. Relationship Between Client and Server

在网络中，经常会看到：

```text
Client
   ↓ Request
Server
   ↓ Response
Client
```

### Client

主动请求服务的一方。

Examples:

- Browser
- Laptop
- Smartphone
- Burp Suite

### Server

提供服务的一方。

Examples:

- Web Server
- DNS Server
- Database Server
- File Server

例如：

```text
Browser
   ↓ HTTP Request
Web Server
   ↓ HTTP Response
Browser
```

---

# Quick Summary

```text
Network
→ 一组可以互相通信的设备

Node
→ 网络中的单个设备

Link
→ 节点之间的通信连接

Data Sharing
→ 网络的主要目的之一

LAN
→ Local Area Network
→ 小范围网络

WAN
→ Wide Area Network
→ 大范围网络

Internet
→ Network of Networks
→ 全球大量网络互相连接

Client
→ 请求服务

Server
→ 提供服务
```

---

# Key Points to Remember

```text
Network = Interconnected Devices

Node = Device

Link = Communication Path

LAN = Small / Local Network

WAN = Large / Wide Network

Internet = Network of Networks

Client = Requests Services

Server = Provides Services
```
